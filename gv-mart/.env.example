# Supabase browser client — anon key only.
# The service_role key must NEVER go here or in any client-side file;
# it lives only in Supabase Edge Function secrets.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
